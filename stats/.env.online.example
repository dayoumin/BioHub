# 온라인 배포용 환경 변수 (CDN 사용)
#
# 사용법:
# 1. 이 파일을 .env.local로 복사 (또는 .env.local 삭제)
#    cp .env.online.example .env.local
#
# 2. 빌드
#    npm run build
#
# 3. 결과: out/ 폴더 (~5 MB)
#    - Pyodide를 CDN에서 로드 (인터넷 필요)
#    - GitHub Pages, Netlify, Vercel 배포 가능

# Pyodide 로컬 사용 비활성화 (CDN 사용)
# NEXT_PUBLIC_PYODIDE_USE_LOCAL=false

# 또는 이 파일을 사용하지 않고 .env.local을 삭제하면 자동으로 CDN 사용
