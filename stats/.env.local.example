# RAG System 환경 변수 (예시)
# .env.local 파일을 생성하고 이 내용을 복사하세요

# =====================================================
# RAG Provider 선택
# =====================================================
# 'claude' (Claude API)
# 'ollama' (완전 로컬, 추천! ⭐)
# 'local' (커스텀 구현)
NEXT_PUBLIC_RAG_PROVIDER=ollama

# =====================================================
# Claude API
# =====================================================
NEXT_PUBLIC_ANTHROPIC_API_KEY=your_claude_api_key_here

# =====================================================
# Ollama (완전 로컬 RAG - 추천! ⭐)
# =====================================================
# Ollama 서버 엔드포인트 (기본: http://localhost:11434)
NEXT_PUBLIC_OLLAMA_ENDPOINT=http://localhost:11434

# 임베딩 모델 (설치된 모델 사용)
# 추천: nomic-embed-text (274 MB, 빠르고 정확)
# 대안: ZimaBlueAI/Qwen3-Embedding-0.6B:f16 (1.2 GB, 더 정확)
NEXT_PUBLIC_OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# 추론 모델 (설치된 모델 사용)
# 추천: qwen3:4b-q4_K_M (2.6 GB, 한국어 우수, 빠름)
# 대안: deepseek-r1:7b (4.7 GB, 추론 능력 우수)
# 대안: exaone-deep (4.8 GB, 한국어 최적화)
NEXT_PUBLIC_OLLAMA_INFERENCE_MODEL=qwen3:4b-q4_K_M

# 벡터 DB 경로 (Week 2에서 생성)
NEXT_PUBLIC_VECTOR_DB_PATH=./rag-system/data/vector_db

# Top-K 검색 결과 수 (기본: 5)
NEXT_PUBLIC_TOP_K=5

# =====================================================
# Ollama 모델 설치 명령어
# =====================================================
# 1. Ollama 설치: https://ollama.com/download
# 2. 임베딩 모델: ollama pull nomic-embed-text
# 3. 추론 모델: ollama pull qwen3:4b-q4_K_M
# 4. (대안) 한국어 특화: ollama pull exaone-deep
# 5. (대안) 추론 능력: ollama pull deepseek-r1:7b

# =====================================================
# 커스텀 로컬 RAG (고급 사용자)
# =====================================================
# NEXT_PUBLIC_EMBEDDING_MODEL_PATH=./rag-system/models/custom-model
# NEXT_PUBLIC_INFERENCE_MODEL=custom

# =====================================================
# OpenRouter API (클라우드 LLM - Smart Flow AI 추천)
# =====================================================
# API 키 발급: https://openrouter.ai/keys
# Fallback 체인: OpenRouter → Ollama → 키워드 기반
NEXT_PUBLIC_OPENROUTER_API_KEY=your_openrouter_api_key_here

# 모델 (⚠️ 필수 — 미지정 시 OpenRouter 비활성화, Ollama → 키워드 fallback으로 전환)
# 쉼표 구분으로 fallback 순서 지정 (첫 번째 실패 → 두 번째 시도 → ...)
# 무료 모델 목록: https://openrouter.ai/models?q=:free
NEXT_PUBLIC_OPENROUTER_MODEL=openai/gpt-oss-120b:free,x-ai/grok-4.1-fast
# 유료 모델 사용 시: NEXT_PUBLIC_OPENROUTER_MODEL=google/gemini-2.5-flash

# =====================================================
# Feedback System (Vercel KV)
# =====================================================
# Vercel KV 사용 시 필요 (Vercel Dashboard에서 발급)
# KV_REST_API_URL=your_kv_rest_api_url
# KV_REST_API_TOKEN=your_kv_rest_api_token

# Feedback Admin API 인증 키 (필수 - 직접 생성)
# FEEDBACK_ADMIN_KEY=your_secure_admin_key_here
