# Multi-Tenant RAG 시스템 계획서

**작성일**: 2025-11-18
**상태**: 📋 기획 단계 (현재 통계 프로젝트 완료 후 착수)
**목적**: 회사 각 부서가 독립적인 RAG 시스템을 운영하고 DB를 공유하는 플랫폼

---

## 📊 사용 사례

### 시나리오
- **각 부서**: 시설, 예산, 청렴, 복무, 계약, 인사, 회계, 법무 등
- **각 부서별 문서 규모**:
  - 심사자료 요약본: 100페이지
  - 참고문헌: 100개 (평균 20페이지) = 2,000페이지
  - 부록: 100개 (평균 12.5페이지) = 1,250페이지
  - **총합**: 3,350페이지 → 4,840 청크

### 워크플로우
1. **시설팀 담당자**: 시설 관련 문서 100+100+100개 → DB 생성 (29MB)
2. **파일 서버 업로드**: 회사 공유 폴더에 DB 파일 업로드
3. **시설팀 직원**: DB 다운로드 → 자신의 PC에 복사 → 즉시 사용 (임베딩 재생성 불필요)
4. **UI에서 DB 선택**: 드롭다운에서 "시설 DB", "예산 DB" 등 선택 → 실시간 전환

---

## 🎯 핵심 요구사항

### 1. DB 공유 체계
- ✅ **부서별 독립 DB**: `rag-facility.db` + `vector-facility-qwen3-0.6b.db`
- ✅ **파일 서버 공유**: 내부망 파일 서버 또는 USB 전달
- ✅ **메타데이터 관리**: DB별 ID, 버전, 생성일, 담당자 정보 ← **중요!**

### 2. UI 개선
- ✅ **DB 선택 드롭다운**: 현재 `VectorStoreSelector` 컴포넌트 활용
- ✅ **실시간 전환**: UI에서 버튼 클릭만으로 부서 DB 전환
- ✅ **메타데이터 표시**: 각 DB의 문서 수, 크기, 최종 업데이트 등

### 3. 체계적 ID 관리
현재는 단순히 파일명으로 구분하지만, **메타데이터 JSON**이 필요:

```json
{
  "id": "facility-v1.2-20250118",
  "department": "시설팀",
  "version": "1.2",
  "createdAt": "2025-01-18T09:00:00Z",
  "createdBy": "홍길동 (facility@company.com)",
  "docCount": 301,
  "pageCount": 3350,
  "chunkCount": 4840,
  "fileSize": "29.2 MB",
  "embeddingModel": "qwen3-embedding:0.6b",
  "description": "2025년 상반기 시설 관리 규정 및 참고자료",
  "tags": ["시설", "건축", "안전", "유지보수"],
  "changelog": "v1.2: 건축법 개정안 반영 (2025-01-15)"
}
```

---

## 🏗️ 아키텍처 설계

### 방식 1: 폴더 복제 (초기 단계, 간편)

```
d:/Projects/
├── Statics/                          # 원본 통계 프로젝트 (그대로 유지)
│   └── statistical-platform/
│
├── CompanyRAG-Facility/              # 시설팀 RAG (복제본)
│   └── statistical-platform/
│       └── public/rag-data/
│           ├── rag.db                # 시설 문서 (15MB)
│           ├── vector-qwen3-0.6b.db  # 임베딩 (14.2MB)
│           └── metadata.json         # 메타데이터 ✨ 추가
│
├── CompanyRAG-Budget/                # 예산팀 RAG
│   └── statistical-platform/
│       └── public/rag-data/
│           ├── rag.db
│           ├── vector-qwen3-0.6b.db
│           └── metadata.json
│
└── CompanyRAG-Shared/                # 공유 파일 서버 (배포용)
    ├── facility/
    │   ├── rag.db
    │   ├── vector-qwen3-0.6b.db
    │   └── metadata.json             # ← 다운로드 전 정보 확인용
    └── budget/
        ├── rag.db
        ├── vector-qwen3-0.6b.db
        └── metadata.json
```

**장점**:
- ✅ 즉시 시작 가능 (복제만 하면 끝)
- ✅ 완전 독립 실행 (한 부서 오류 → 다른 부서 무영향)
- ✅ 코드 수정 불필요

**단점**:
- ❌ 프로젝트 폴더 중복 (8개 부서 → 8개 폴더)
- ❌ 업데이트 시 모든 폴더 동기화 필요

---

### 방식 2: 단일 프로젝트 + DB 전환 (최종 단계, 권장) ⭐

```
d:/Projects/CompanyRAG/
├── statistical-platform/             # 단일 프로젝트
│   ├── public/rag-data/
│   │   ├── facility/                 # 시설팀 DB
│   │   │   ├── rag.db
│   │   │   ├── vector-qwen3-0.6b.db
│   │   │   └── metadata.json
│   │   ├── budget/                   # 예산팀 DB
│   │   │   ├── rag.db
│   │   │   ├── vector-qwen3-0.6b.db
│   │   │   └── metadata.json
│   │   ├── contract/                 # 계약팀 DB
│   │   │   └── ...
│   │   └── db-registry.json          # 전체 DB 목록 ✨ 추가
│   │
│   └── app/(dashboard)/rag/
│       └── settings/page.tsx         # UI 개선: DB 선택 드롭다운
```

**`db-registry.json` 예시**:
```json
{
  "databases": [
    {
      "id": "facility-v1.2",
      "path": "/rag-data/facility/",
      "department": "시설팀",
      "status": "active",
      "metadata": "./facility/metadata.json"
    },
    {
      "id": "budget-v2.0",
      "path": "/rag-data/budget/",
      "department": "예산팀",
      "status": "active",
      "metadata": "./budget/metadata.json"
    }
  ],
  "default": "facility-v1.2"
}
```

**장점**:
- ✅ 단일 프로젝트 관리 (업데이트 간편)
- ✅ UI에서 DB 실시간 전환
- ✅ 메타데이터 중앙 관리

**단점**:
- ❌ 초기 구현 복잡도 높음 (UI 개선 + DB 전환 로직)

---

## 🛠️ 구현 계획

### Phase 1: 메타데이터 시스템 구축 ✨

**목표**: DB별 ID, 버전, 담당자 정보 관리

**작업**:
1. `metadata.json` 스키마 정의
2. DB 생성 시 자동으로 메타데이터 생성
3. `getAvailableVectorStores()` 함수 개선
   - 현재: 파일명만 파싱 (`vector-qwen3-*.db`)
   - 개선: `metadata.json` 읽어서 상세 정보 표시

**파일**:
- `lib/rag/utils/metadata-manager.ts` (신규)
- `lib/rag/rag-service.ts` (수정)

---

### Phase 2: UI 개선 (DB 선택 드롭다운)

**목표**: 설정 페이지에서 부서별 DB 선택 가능

**작업**:
1. `VectorStoreSelector` 컴포넌트 개선
   - 현재: 임베딩 모델별 구분
   - 개선: 부서별 구분 + 메타데이터 표시
2. `app/(dashboard)/settings/page.tsx` 개선
   - RAG 탭에 "Vector Store" 섹션 추가
   - 드롭다운에서 선택 → `RAGService.initialize()` 호출

**UI 예시**:
```
┌─────────────────────────────────────────┐
│ Vector Store 선택                        │
├─────────────────────────────────────────┤
│ ▼ 시설팀 v1.2 (301개 문서, 29MB)        │
│   - 최종 업데이트: 2025-01-18           │
│   - 담당자: 홍길동 (facility@company)   │
│   - 설명: 2025 상반기 시설 관리 규정    │
├─────────────────────────────────────────┤
│ □ 예산팀 v2.0 (450개 문서, 40MB)        │
│ □ 계약팀 v1.0 (280개 문서, 25MB)        │
└─────────────────────────────────────────┘

[적용] [취소]
```

---

### Phase 3: DB 공유 자동화

**목표**: 파일 서버 업로드/다운로드 자동화

**작업**:
1. **업로드 스크립트** (`scripts/upload-db.sh`)
   ```bash
   #!/bin/bash
   # 시설팀 담당자가 DB 생성 후
   DEPT_ID="facility"
   VERSION="v1.2"

   # 메타데이터 검증
   node scripts/validate-metadata.js public/rag-data/metadata.json

   # 파일 서버 업로드
   scp public/rag-data/rag.db fileserver:/shared/rag/${DEPT_ID}-${VERSION}/
   scp public/rag-data/vector-*.db fileserver:/shared/rag/${DEPT_ID}-${VERSION}/
   scp public/rag-data/metadata.json fileserver:/shared/rag/${DEPT_ID}-${VERSION}/

   echo "✅ 업로드 완료: ${DEPT_ID} ${VERSION}"
   ```

2. **다운로드 스크립트** (`scripts/download-db.sh`)
   ```bash
   #!/bin/bash
   # 사용법: ./download-db.sh facility v1.2

   DEPT_ID=$1
   VERSION=$2

   # 메타데이터 먼저 확인
   curl http://fileserver/shared/rag/${DEPT_ID}-${VERSION}/metadata.json

   read -p "다운로드하시겠습니까? (y/n) " -n 1 -r
   if [[ $REPLY =~ ^[Yy]$ ]]; then
     mkdir -p public/rag-data/${DEPT_ID}/
     curl http://fileserver/.../rag.db -o public/rag-data/${DEPT_ID}/
     curl http://fileserver/.../vector-*.db -o public/rag-data/${DEPT_ID}/
     curl http://fileserver/.../metadata.json -o public/rag-data/${DEPT_ID}/
     echo "✅ 다운로드 완료"
   fi
   ```

3. **UI 다운로드 기능** (선택)
   - 설정 페이지에 "파일 서버에서 DB 가져오기" 버튼
   - 사용 가능한 DB 목록 표시 (메타데이터 기반)
   - 클릭 시 백그라운드 다운로드

---

### Phase 4: 버전 관리 시스템

**목표**: DB 업데이트 시 버전 관리 + 변경 이력

**작업**:
1. **Semantic Versioning 적용**
   - `v1.0.0`: 초기 버전
   - `v1.1.0`: 문서 추가 (마이너)
   - `v1.1.1`: 문서 수정 (패치)
   - `v2.0.0`: 대규모 개편 (메이저)

2. **Changelog 자동 생성**
   ```json
   {
     "changelog": [
       {
         "version": "v1.2.0",
         "date": "2025-01-18",
         "changes": [
           "건축법 개정안 반영 (50개 문서 추가)",
           "구 규정 아카이브 (20개 문서 삭제)"
         ],
         "author": "홍길동"
       },
       {
         "version": "v1.1.0",
         "date": "2025-01-10",
         "changes": ["안전 매뉴얼 업데이트"],
         "author": "김철수"
       }
     ]
   }
   ```

3. **호환성 체크**
   - 다운로드 시 로컬 버전 vs 서버 버전 비교
   - 업데이트 알림 표시

---

## 📋 체크리스트 (착수 전 확인)

### 전제 조건
- [ ] 현재 통계 프로젝트 완료 (Phase 9-12)
- [ ] 현재 RAG 시스템 안정화 (버그 없음)
- [ ] 성능 테스트 완료 (3,350페이지 검색 < 0.5초)

### 기술 검증
- [ ] SQLite 브라우저 제한 확인 (최대 DB 크기)
- [ ] 여러 DB 동시 로드 시 메모리 사용량 측정
- [ ] 파일 서버 접근 권한 설정 (CORS, 인증)

### 사용자 요구사항
- [ ] 부서 목록 확정 (시설, 예산, 계약, ...)
- [ ] DB 공유 방식 합의 (파일 서버 vs USB vs 이메일)
- [ ] 메타데이터 필수 항목 확정
- [ ] 담당자 교육 계획 (DB 생성, 업로드, 버전 관리)

---

## 🎯 최종 목표

**완성된 시스템**:
1. ✅ 각 부서가 **독립적으로 DB 생성**
2. ✅ **파일 서버에 공유** (메타데이터 포함)
3. ✅ 다른 직원이 **UI에서 선택** → 즉시 사용
4. ✅ **버전 관리** (v1.0 → v1.1 → v2.0)
5. ✅ **변경 이력 추적** (누가, 언제, 무엇을 변경)

**성공 기준**:
- 부서별 DB 생성 소요 시간: < 30분
- DB 공유 소요 시간: < 5분 (다운로드)
- UI에서 DB 전환 소요 시간: < 2초
- 메타데이터 자동 생성: 100%

---

## 📚 참고 자료

### 현재 시스템 구조
- [rag-service.ts](../statistical-platform/lib/rag/rag-service.ts) - RAG 서비스 (vectorStoreId 지원)
- [vector-store-selector.tsx](../statistical-platform/components/rag/vector-store-selector.tsx) - UI 컴포넌트
- [getAvailableVectorStores()](../statistical-platform/lib/rag/rag-service.ts#L372-L388) - DB 목록 조회

### 성능 분석
- 3,350페이지 → 4,840 청크
- DB 크기: 29.2MB (메타 15MB + 벡터 14.2MB)
- 검색 속도: ~242ms (충분히 빠름)
- 메모리 사용: ~58MB (브라우저 제한의 2.9%)

### 확장성 한계
- 현재 방식 한계: ~34,607페이지 (검색 500ms 기준)
- 패턴 전환 필요 시점: 10만 페이지 이상 (PostgreSQL + pgvector)

---

**작성자**: Claude Code
**최종 수정**: 2025-01-18
**다음 단계**: 현재 통계 프로젝트 완료 후 Phase 1 착수
