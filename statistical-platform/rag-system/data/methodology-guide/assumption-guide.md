# 통계 가정 검증 및 대안 가이드

**작성일**: 2025-10-31
**대상**: 석박사 연구자
**목적**: 통계 가정 위반 시 대처 방법

---

## 📋 목차

1. [정규성 가정 (Normality)](#1-정규성-가정-normality)
2. [등분산성 가정 (Homogeneity of Variance)](#2-등분산성-가정-homogeneity-of-variance)
3. [독립성 가정 (Independence)](#3-독립성-가정-independence)
4. [구형성 가정 (Sphericity)](#4-구형성-가정-sphericity)
5. [회귀분석 특수 가정](#5-회귀분석-특수-가정)
6. [종합 의사결정 트리](#6-종합-의사결정-트리)

---

## 1. 정규성 가정 (Normality)

### 1.1 정의
데이터가 정규분포(Normal Distribution)를 따르는지 확인

### 1.2 검증 방법

#### A. 통계적 검정
```
샘플 크기에 따른 선택:

┌─────────────────────────────────────┐
│ n < 50 → Shapiro-Wilk Test          │
│ n ≥ 50 → Kolmogorov-Smirnov Test    │
└─────────────────────────────────────┘

해석:
- p > 0.05 → 정규분포 (가정 충족)
- p ≤ 0.05 → 정규성 위반 (가정 불충족)
```

**검색 키워드**: 'shapiro', 'ks-test'
**카테고리**: 정규성 검정

#### B. 시각적 확인 (권장)
1. **Q-Q Plot (Quantile-Quantile Plot)**
   - 정규분포면 점들이 대각선에 위치
   - 꼬리 부분 벗어남 → 왜도/첨도 문제

2. **히스토그램**
   - 종 모양(Bell curve) 확인
   - 왜도(Skewness), 첨도(Kurtosis) 시각화

3. **박스플롯 (Box Plot)**
   - 이상치 확인
   - 중위수 위치 (대칭성)

---

### 1.3 정규성 위반 시 대처

#### Option A: 비모수 검정 사용 (가장 안전)

| 모수 검정 | 비모수 대안 | 검색 키워드 |
|----------|------------|------------|
| 독립표본 t-검정 | Mann-Whitney U | 'mann-whitney' |
| 대응표본 t-검정 | Wilcoxon Signed-Rank | 'wilcoxon' |
| 일원 ANOVA | Kruskal-Wallis | 'kruskal-wallis' |
| 반복측정 ANOVA | Friedman 검정 | 'friedman' |
| Pearson 상관 | Spearman 순위상관 | 'spearman' |

**장점**: 정규성 가정 불필요, 이상치에 강건
**단점**: 검정력(Power) 약간 낮음

---

#### Option B: 데이터 변환 (Transformation)

**1. 로그 변환 (Log Transformation)**
```python
import numpy as np

# 우측 왜도(right-skewed) 데이터에 효과적
log_data = np.log(data + 1)  # +1은 0 방지
```

**사용 시기**:
- 우측 왜도 (오른쪽 꼬리가 긴 분포)
- 양수 데이터만 가능
- 예: 소득, 반응 시간, 농도

**2. 제곱근 변환 (Square Root)**
```python
sqrt_data = np.sqrt(data)
```

**사용 시기**:
- 약한 우측 왜도
- 카운트 데이터 (0 포함 가능)
- 예: 세포 수, 사건 발생 횟수

**3. Box-Cox 변환**
```python
from scipy.stats import boxcox

transformed_data, lambda_param = boxcox(data)
```

**사용 시기**:
- 최적 변환 자동 탐색
- 양수 데이터만 가능

**주의**: 변환 후 해석이 어려워질 수 있음 (예: log(소득)의 차이 ≠ 소득 차이)

---

#### Option C: 중심극한정리 (Central Limit Theorem) 활용

```
대규모 샘플 (n ≥ 30)에서는 표본 평균의 분포가
정규분포에 근사

→ t-검정/ANOVA 사용 가능 (강건성)
```

**조건**:
- ✅ n ≥ 30 (각 그룹)
- ✅ 극단적 왜도 없음
- ✅ 이상치 제거됨

**검증**: Bootstrap 신뢰구간으로 확인

---

#### Option D: 순열 검정 (Permutation Test)

```python
from scipy.stats import permutation_test

# 분포 가정 없이 p-value 계산
result = permutation_test((group1, group2), statistic, ...)
```

**장점**:
- 어떤 분포에도 사용 가능
- 정확한 p-value

**단점**:
- 계산 비용 높음 (반복 시뮬레이션)

---

### 1.4 의사결정 플로우차트

```
시작: 정규성 검정 (Shapiro-Wilk/K-S)
│
├─ p > 0.05 (정규분포)
│   └─ 모수 검정 사용 (t-test, ANOVA 등)
│
└─ p ≤ 0.05 (정규성 위반)
    │
    ├─ 샘플 크기는?
    │   ├─ n ≥ 30 → 중심극한정리
    │   │   └─ 모수 검정 사용 (강건)
    │   └─ n < 30 → 다음 단계
    │
    ├─ Q-Q Plot 확인
    │   ├─ 약한 왜도 → 변환 시도
    │   │   ├─ 로그 변환
    │   │   ├─ 제곱근 변환
    │   │   └─ Box-Cox 변환
    │   │
    │   └─ 강한 왜도 또는 이상치 다수
    │       └─ 비모수 검정 (추천 ⭐)
    │
    └─ 해석의 명확성 중요?
        ├─ Yes → 비모수 검정
        └─ No → 변환 가능
```

---

## 2. 등분산성 가정 (Homogeneity of Variance)

### 2.1 정의
여러 그룹의 분산이 동일한지 확인

### 2.2 검증 방법

#### A. Levene's Test (권장)

```python
from scipy.stats import levene

statistic, p_value = levene(group1, group2, group3)
```

**해석**:
- p > 0.05 → 등분산 (가정 충족)
- p ≤ 0.05 → 등분산 위반

**장점**: 정규성 가정 불필요 (강건)

**검색 키워드**: 'levene'
**카테고리**: 가정 검증

---

#### B. Bartlett's Test

```python
from scipy.stats import bartlett

statistic, p_value = bartlett(group1, group2, group3)
```

**사용 조건**: 데이터가 정규분포일 때만
**단점**: 정규성에 민감

---

#### C. 시각적 확인

1. **박스플롯 (Box Plot)**
   - 각 그룹의 IQR(상자 크기) 비교
   - 유사한 크기면 등분산 가능성 높음

2. **Residual Plot** (ANOVA/회귀분석)
   - 잔차가 0 주위에 균일하게 분포

---

### 2.3 등분산 위반 시 대처

#### Option A: Welch's t-test (두 그룹 비교)

```python
from scipy.stats import ttest_ind

# welch=True로 등분산 가정 무시
statistic, p_value = ttest_ind(group1, group2, equal_var=False)
```

**검색 키워드**: 'welch-t'
**장점**: 등분산 불필요, 검정력 유지

---

#### Option B: Welch's ANOVA (세 그룹 이상)

```python
from scipy.stats import f_oneway

# 표준 scipy는 Welch ANOVA 미지원
# → statsmodels 또는 pingouin 사용
import pingouin as pg

result = pg.welch_anova(dv='value', between='group', data=df)
```

**검색 키워드**: 'welch-anova'
**장점**: 등분산 불필요

---

#### Option C: 비모수 검정

| 모수 검정 | 비모수 대안 |
|----------|------------|
| 독립표본 t-검정 | Mann-Whitney U |
| 일원 ANOVA | Kruskal-Wallis |

---

#### Option D: 변환 (Transformation)

**로그 변환이 등분산 개선에 효과적**:
```python
log_data = np.log(data + 1)
# 변환 후 Levene's Test 재검증
```

---

### 2.4 의사결정 플로우차트

```
시작: Levene's Test
│
├─ p > 0.05 (등분산)
│   └─ 표준 t-test/ANOVA 사용
│
└─ p ≤ 0.05 (등분산 위반)
    │
    ├─ 두 그룹 비교?
    │   └─ Welch's t-test 사용 ⭐
    │
    ├─ 세 그룹 이상?
    │   ├─ Welch's ANOVA
    │   └─ 또는 Kruskal-Wallis (비모수)
    │
    └─ 변환 시도?
        └─ 로그 변환 → 재검증
```

---

## 3. 독립성 가정 (Independence)

### 3.1 정의
각 관측치가 서로 독립적 (한 관측치가 다른 관측치에 영향 없음)

### 3.2 검증 방법

#### A. 연구 설계 확인 (가장 중요)

**독립성 충족**:
- ✅ 무작위 샘플링 (Random Sampling)
- ✅ 각 참가자가 1번만 측정됨
- ✅ 그룹 간 참가자 중복 없음

**독립성 위반**:
- ❌ 반복측정 (Repeated Measures) - 같은 사람의 전후 비교
- ❌ 짝지어진 데이터 (Paired Data) - 쌍둥이, 부부
- ❌ 클러스터 샘플링 (Cluster Sampling) - 학급 단위 샘플링
- ❌ 시계열 데이터 (Time Series) - 자기상관 (Autocorrelation)

---

#### B. 통계적 검정 (회귀분석 전용)

**Durbin-Watson Test** - 잔차 자기상관 검정

```python
from statsmodels.stats.stattools import durbin_watson

dw_statistic = durbin_watson(residuals)
```

**해석**:
- DW ≈ 2 → 독립성 충족
- DW < 1.5 → 양의 자기상관 (연속된 잔차가 유사)
- DW > 2.5 → 음의 자기상관 (연속된 잔차가 반대)

**검색 키워드**: 'durbin-watson'
**카테고리**: 회귀분석 진단

---

### 3.3 독립성 위반 시 대처

#### Option A: 쌍 데이터 분석 사용

| 상황 | 올바른 방법 |
|------|------------|
| 전후 비교 (Before-After) | 대응표본 t-검정 |
| 정규성 위반 전후 비교 | Wilcoxon Signed-Rank |
| 세 시점 이상 반복측정 | 반복측정 ANOVA |

**검색 키워드**: 'paired-t', 'wilcoxon', 'repeated-measures'

---

#### Option B: 혼합 모형 (Mixed Model)

```
다층 구조 데이터 (Hierarchical Data)
예: 학생 ⊂ 학급 ⊂ 학교

→ 선형혼합모형 (Linear Mixed Model, LMM)
→ 일반화선형혼합모형 (GLMM)
```

**검색 키워드**: 'mixed-model', 'lmm'
**카테고리**: 고급 분석

---

#### Option C: 시계열 분석

```
자기상관 있는 시계열 데이터
→ ARIMA, VAR 모형
```

**검색 키워드**: 'time-series', 'arima'
**카테고리**: 시계열 분석

---

### 3.4 의사결정 플로우차트

```
시작: 연구 설계 확인
│
├─ 각 관측치 독립?
│   └─ Yes → 표준 분석 사용
│
└─ No (독립성 위반)
    │
    ├─ 쌍 데이터? (n=2 측정)
    │   └─ 대응표본 t-검정 또는 Wilcoxon
    │
    ├─ 반복측정? (n≥3 측정)
    │   └─ 반복측정 ANOVA 또는 Friedman
    │
    ├─ 클러스터 구조?
    │   └─ 혼합 모형 (LMM, GLMM)
    │
    └─ 시계열?
        └─ 시계열 분석 (ARIMA 등)
```

---

## 4. 구형성 가정 (Sphericity)

### 4.1 정의
**반복측정 ANOVA 전용 가정**

각 시점 간 차이의 분산이 모두 동일 (Variance of differences)

### 4.2 검증 방법

#### Mauchly's Test of Sphericity

```python
# statsmodels 또는 pingouin 사용
import pingouin as pg

result = pg.rm_anova(dv='value', within='time', subject='id', data=df)
# result['sphericity'] 확인
```

**해석**:
- p > 0.05 → 구형성 충족
- p ≤ 0.05 → 구형성 위반

**검색 키워드**: 'mauchly', 'sphericity'
**카테고리**: 반복측정 ANOVA

---

### 4.3 구형성 위반 시 대처

#### Option A: Greenhouse-Geisser 보정 (권장)

```python
# 자유도 조정 (더 보수적)
# epsilon 값에 따라 p-value 재계산
```

**사용 시기**: epsilon < 0.75 (중간~심한 위반)

---

#### Option B: Huynh-Feldt 보정

**사용 시기**: epsilon > 0.75 (약한 위반)

---

#### Option C: Friedman 검정 (비모수)

```python
from scipy.stats import friedmanchisquare

statistic, p_value = friedmanchisquare(time1, time2, time3)
```

**장점**: 구형성 가정 불필요
**단점**: 검정력 낮음

**검색 키워드**: 'friedman'

---

### 4.4 의사결정 플로우차트

```
시작: Mauchly's Test
│
├─ p > 0.05 (구형성 충족)
│   └─ 표준 반복측정 ANOVA
│
└─ p ≤ 0.05 (구형성 위반)
    │
    ├─ epsilon (ε) 값은?
    │   ├─ ε < 0.75 → Greenhouse-Geisser 보정 ⭐
    │   └─ ε > 0.75 → Huynh-Feldt 보정
    │
    └─ 또는 비모수 검정
        └─ Friedman 검정
```

---

## 5. 회귀분석 특수 가정

### 5.1 선형성 (Linearity)

#### 정의
독립변수와 종속변수 간 선형 관계

#### 검증 방법

**Residual vs Fitted Plot**
```python
import matplotlib.pyplot as plt

plt.scatter(fitted_values, residuals)
plt.axhline(y=0, color='r', linestyle='--')
plt.xlabel('Fitted values')
plt.ylabel('Residuals')
```

**판단 기준**:
- ✅ 잔차가 0 주위에 무작위 분포
- ❌ 패턴 보임 (곡선, U자형) → 비선형 관계

---

#### 위반 시 대처

**Option A: 다항 회귀 (Polynomial Regression)**
```python
# X, X², X³ 항 추가
from sklearn.preprocessing import PolynomialFeatures

poly = PolynomialFeatures(degree=2)
X_poly = poly.fit_transform(X)
```

**Option B: 비선형 변환**
- 로그 변환: `log(Y)`, `log(X)`
- 지수 변환: `exp(X)`

**Option C: 비선형 회귀**
- GAM (Generalized Additive Model)
- Spline regression

---

### 5.2 다중공선성 (Multicollinearity)

#### 정의
독립변수 간 높은 상관 (다중 회귀 전용)

#### 검증 방법

**VIF (Variance Inflation Factor)**
```python
from statsmodels.stats.outliers_influence import variance_inflation_factor

vif = [variance_inflation_factor(X.values, i) for i in range(X.shape[1])]
```

**판단 기준**:
- ✅ VIF < 5 (엄격 기준)
- ⚠️ VIF 5-10 (주의)
- ❌ VIF > 10 (심각한 다중공선성)

---

#### 위반 시 대처

**Option A: 변수 제거**
- VIF 가장 높은 변수 제거
- 반복하여 VIF < 10 달성

**Option B: 주성분 분석 (PCA)**
```python
from sklearn.decomposition import PCA

pca = PCA(n_components=5)
X_pca = pca.fit_transform(X)
```

**Option C: Ridge/Lasso 회귀 (정규화)**
```python
from sklearn.linear_model import Ridge, Lasso

ridge = Ridge(alpha=1.0)
ridge.fit(X, y)
```

---

### 5.3 잔차 정규성 (Normality of Residuals)

#### 검증 방법

**Q-Q Plot of Residuals**
```python
import scipy.stats as stats
import matplotlib.pyplot as plt

stats.probplot(residuals, dist="norm", plot=plt)
```

#### 위반 시 대처

1. **종속변수 변환** (로그, Box-Cox)
2. **이상치 제거**
3. **비모수 부트스트랩**

---

## 6. 종합 의사결정 트리

### 6.1 가정 검증 순서

```
Step 1: 연구 설계 확인
├─ 독립성 검증
│   ├─ 충족 → 다음 단계
│   └─ 위반 → 쌍 데이터/혼합 모형

Step 2: 데이터 분포 확인
├─ 정규성 검정 (Shapiro-Wilk)
│   ├─ 충족 → 다음 단계
│   └─ 위반 → 변환/비모수 검정

Step 3: 그룹 간 분산 확인 (비교 연구 only)
├─ 등분산성 검정 (Levene)
│   ├─ 충족 → 모수 검정 사용
│   └─ 위반 → Welch 방법

Step 4: 특수 가정 (해당 시)
├─ 반복측정 ANOVA → 구형성 (Mauchly)
├─ 회귀분석 → 선형성, 다중공선성, 잔차 정규성
```

---

### 6.2 Quick Reference 표

| 검정 | 주요 가정 | 검증 방법 | 위반 시 대안 |
|------|----------|----------|-------------|
| **독립표본 t-검정** | 정규성, 등분산, 독립성 | Shapiro-Wilk, Levene | Welch's t-test, Mann-Whitney U |
| **대응표본 t-검정** | 차이값 정규성 | Shapiro-Wilk (차이) | Wilcoxon Signed-Rank |
| **일원 ANOVA** | 정규성, 등분산, 독립성 | Shapiro-Wilk, Levene | Kruskal-Wallis, Welch ANOVA |
| **반복측정 ANOVA** | 정규성, 구형성 | Shapiro-Wilk, Mauchly | Friedman, GG 보정 |
| **Pearson 상관** | 양 변수 정규분포 | Shapiro-Wilk × 2 | Spearman 순위상관 |
| **선형 회귀** | 선형성, 정규성(잔차), 등분산(잔차), 독립성 | Residual plots, Durbin-Watson | 변환, 비선형 회귀 |
| **다중 회귀** | + 다중공선성 | VIF | 변수 제거, Ridge/Lasso |

---

## 7. 실전 체크리스트

### 7.1 분석 전 확인사항

**데이터 정제**:
- [ ] 결측치 처리 (제거 또는 대체)
- [ ] 이상치 확인 (박스플롯, Z-score > 3)
- [ ] 데이터 타입 확인 (연속형 vs 범주형)

**가정 검증**:
- [ ] 독립성: 연구 설계 확인
- [ ] 정규성: Shapiro-Wilk + Q-Q Plot
- [ ] 등분산성: Levene's Test (비교 연구)
- [ ] 구형성: Mauchly's Test (반복측정)

---

### 7.2 위반 시 우선순위

**1순위**: 비모수 검정 (가장 안전)
**2순위**: Welch 방법 (등분산 불필요)
**3순위**: 데이터 변환 (해석 복잡)
**4순위**: 중심극한정리 (n ≥ 30)

---

## 8. 참고 문헌

**통계 가정**:
- Ghasemi, A., & Zahediasl, S. (2012). Normality tests for statistical analysis: a guide for non-statisticians. *International Journal of Endocrinology and Metabolism*, 10(2), 486.
- Blanca, M. J., et al. (2017). Non-normal data: Is ANOVA still a valid option? *Psicothema*, 29(4), 552-557.

**비모수 검정**:
- Conover, W. J. (1999). *Practical Nonparametric Statistics* (3rd ed.). Wiley.

**회귀분석 진단**:
- Fox, J. (2015). *Applied Regression Analysis and Generalized Linear Models* (3rd ed.). SAGE Publications.

---

**작성자**: Claude Code (AI)
**버전**: 1.0
**최종 수정**: 2025-10-31
