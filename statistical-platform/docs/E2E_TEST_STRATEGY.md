# E2E 테스트 전략 가이드

> **검증일**: 2026-01-30
> **참조**: [Playwright 공식 문서](https://playwright.dev/docs/test-agents), [Agent Browser](https://github.com/vercel-labs/agent-browser)

---

## 📊 도구 비교표 (핵심)

| 항목 | Playwright | Playwright Test Agents | Agent Browser |
|------|-----------|----------------------|---------------|
| **정체** | 테스트 프레임워크 | AI 테스트 생성 시스템 | AI용 브라우저 CLI |
| **릴리즈** | 2020~ | v1.56 (2025.10) | 2025~ |
| **사용자** | 개발자가 직접 작성 | AI가 코드 생성 | AI가 직접 제어 |
| **출력** | `.spec.ts` 테스트 파일 | `.spec.ts` 테스트 파일 | 실시간 브라우저 조작 |
| **용도** | CI/CD 자동화 | 테스트 코드 자동 생성 | 탐색/디버깅/일회성 테스트 |
| **토큰 효율** | 낮음 (DOM 전체) | 중간 | 높음 (93% 절감) |
| **설치** | 프로젝트별 | 프로젝트별 | 전역 CLI |

**결론**: 서로 **대체 관계가 아닌 보완 관계**

---

## 🎭 Playwright Test Agents 상세

### 개요
Playwright v1.56부터 도입된 AI 기반 테스트 자동화 시스템. MCP(Model Context Protocol)를 통해 Claude Code, VS Code Copilot 등과 연동.

### 3가지 Agent 역할

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Planner   │ →  │  Generator  │ →  │   Healer    │
│  (계획 수립) │    │ (코드 생성)  │    │ (자동 수정)  │
└─────────────┘    └─────────────┘    └─────────────┘
     │                   │                   │
     ▼                   ▼                   ▼
 specs/*.md        tests/*.spec.ts    (실패 시 자동 복구)
```

| Agent | 입력 | 출력 | 역할 |
|-------|-----|------|------|
| **Planner** | 요청사항 + 시드 테스트 + PRD(선택) | `specs/*.md` 마크다운 계획 | 앱 탐색 → 테스트 시나리오 작성 |
| **Generator** | `specs/*.md` 마크다운 파일 | `tests/*.spec.ts` 테스트 파일 | 계획 → 실행 가능한 Playwright 코드 변환 |
| **Healer** | 실패한 테스트 | 수정된 테스트 또는 스킵 | 실패 분석 → 셀렉터 업데이트 → 재실행 |

### 설치 및 초기화

```bash
# VS Code Copilot 사용 (v1.105 이상 필요)
npx playwright init-agents --loop=vscode

# Claude Code 사용 (CLI 또는 VS Code 확장)
npx playwright init-agents --loop=claude

# OpenCode 사용
npx playwright init-agents --loop=opencode
```

### `--loop` 옵션 상세

| 옵션 | 환경 | AI 플랫폼 | 요구사항 |
|------|------|----------|----------|
| `--loop=vscode` | VS Code | GitHub Copilot Chat | VS Code v1.105+ |
| `--loop=claude` | VS Code + Claude Code 확장 | Claude | - |
| `--loop=claude` | 터미널 + Claude Code CLI | Claude | - |
| `--loop=opencode` | OpenCode | OpenCode | - |

> **참고**: VS Code에서 Claude Code 확장을 사용하는 경우 `--loop=claude`를 선택하세요.

### VS Code + Claude Code 확장 사용법

**환경 구성:**
```
VS Code
  └── Claude Code 확장 (Extension)
        └── MCP (Model Context Protocol)
              └── Playwright Agents (Planner/Generator/Healer)
                    └── 브라우저 (Chromium)
```

**작동 방식:**
```
┌─────────────────────────────────────────────────────────────┐
│  VS Code + Claude Code Extension                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  사용자 입력 (자연어)                                        │
│  "이 앱의 로그인 테스트를 만들어줘"                           │
│                    │                                        │
│                    ▼                                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Claude Code Extension                              │   │
│  │  (MCP 클라이언트)                                    │   │
│  └───────────────────────┬─────────────────────────────┘   │
│                          │ MCP Protocol                     │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Playwright MCP Server                              │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐               │   │
│  │  │ Planner │ │Generator│ │ Healer  │               │   │
│  │  └─────────┘ └─────────┘ └─────────┘               │   │
│  └───────────────────────┬─────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  브라우저 (Chromium)                                 │   │
│  │  → 실제 앱 탐색 및 테스트 실행                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**실제 사용 예시:**

1. **초기화 (프로젝트당 한 번)**
   ```bash
   cd your-project
   npx playwright init-agents --loop=claude
   ```

2. **VS Code에서 Claude Code 확장 열기**
   - `Ctrl+Shift+P` → "Claude Code" 검색
   - 또는 사이드바의 Claude Code 아이콘 클릭

3. **자연어로 Agent 호출**
   ```
   # Planner 실행
   "이 앱의 테스트 계획을 만들어줘"
   → specs/*.md 생성

   # Generator 실행
   "specs 폴더의 계획으로 테스트 코드 생성해줘"
   → tests/*.spec.ts 생성

   # Healer 실행
   "실패한 테스트 수정해줘"
   → 테스트 자동 복구
   ```

4. **Agent 정의 커스터마이징 (선택)**
   - `.github/` 폴더의 Agent 정의 파일 수정 가능
   - Planner 탐색 전략, Generator 코드 스타일 등 조정

### 생성되는 디렉토리 구조

```
project/
├── .github/              # Agent 정의 (MCP 도구)
├── specs/                # 마크다운 테스트 계획
│   └── basic-operations.md
├── tests/                # 생성된 Playwright 테스트
│   ├── seed.spec.ts      # 환경 초기화 (시드 테스트)
│   └── feature/
│       └── login.spec.ts
└── playwright.config.ts
```

### MCP (Model Context Protocol) 연동

```
┌──────────────────┐
│  AI 플랫폼       │
│  (Claude/Copilot)│
└────────┬─────────┘
         │ MCP
         ▼
┌──────────────────┐
│ Playwright Agents│
│ (Planner/Gen/Heal)│
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│   브라우저       │
│   (Chromium)     │
└──────────────────┘
```

### MCP 서버 옵션

| 서버 | 패키지 | 특징 |
|------|--------|------|
| **Microsoft 공식** | `@playwright/mcp` | 공식 유지보수, 안정적 |
| **ExecuteAutomation** | `@executeautomation/playwright-mcp-server` | 커뮤니티, API 테스트 지원 |

**설치 (Claude Code):**
```bash
# Microsoft 공식
claude mcp add playwright npx @playwright/mcp@latest

# ExecuteAutomation (API 테스트 포함)
claude mcp add playwright npx @executeautomation/playwright-mcp-server
```

---

## 🌐 Agent Browser 상세

### 개요
Vercel Labs에서 개발한 AI 에이전트 전용 브라우저 CLI. 토큰 효율성에 최적화.

### 핵심 혁신: Snapshot + Refs 시스템

```
기존 Playwright MCP:
┌────────────────────────────────────┐
│ 전체 Accessibility Tree           │
│ (15,000+ 토큰)                    │
└────────────────────────────────────┘

Agent Browser:
┌────────────────────────────────────┐
│ @e1: Button "Login"               │
│ @e2: Input "Email"                │
│ @e3: Input "Password"             │
│ (300 토큰)                        │
└────────────────────────────────────┘
→ 93% 토큰 절감!
```

### 설치

```bash
# 전역 설치 (권장)
npm install -g agent-browser
agent-browser install  # Chromium 다운로드

# Linux 시스템 의존성 포함
agent-browser install --with-deps
```

### 주요 명령어

| 카테고리 | 명령어 | 설명 |
|---------|--------|------|
| **네비게이션** | `open <url>` | URL 열기 |
| **스냅샷** | `snapshot` | AI용 접근성 트리 (Refs 포함) |
| | `snapshot -i` | Interactive 요소만 |
| | `snapshot -c` | Compact 모드 |
| | `snapshot -d 3` | 깊이 제한 |
| **상호작용** | `click @e1` | Ref로 요소 클릭 |
| | `fill @e2 "text"` | Ref로 입력 |
| | `press Enter` | 키보드 입력 |
| **Semantic Locators** | `find role button click` | ARIA role로 찾기 |
| | `find text "Login" click` | 텍스트로 찾기 |
| | `find label "Email" fill "test@example.com"` | 라벨로 찾기 |
| **추출** | `get text @e3` | 텍스트 추출 |
| | `get html @e3` | HTML 추출 |
| | `get value @e3` | 입력값 추출 |
| **디버깅** | `screenshot` | 스크린샷 |
| | `errors` | 페이지 에러 확인 |
| | `trace start` | 추적 시작 |
| **세션** | `--session agent1` | 격리된 세션 |
| | `--profile ~/.profile` | 영구 프로필 |

### 사용 사례

1. **탐색/디버깅**: 앱 구조 빠르게 파악
2. **일회성 테스트**: 반복 실행 불필요한 검증
3. **AI 자동화**: LLM이 snapshot 분석 후 행동 결정

---

## 🏗️ 권장 E2E 테스트 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    E2E 테스트 워크플로우                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Phase 1: 계획                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Playwright Test Agents (Planner)                    │   │
│  │ → specs/*.md 테스트 계획 자동 생성                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                         ↓                                   │
│  Phase 2: 검토 (선택)                                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Agent Browser                                       │   │
│  │ → 수동 탐색으로 계획 검증                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                         ↓                                   │
│  Phase 3: 생성                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Playwright Test Agents (Generator)                  │   │
│  │ → tests/*.spec.ts 테스트 코드 자동 생성              │   │
│  └─────────────────────────────────────────────────────┘   │
│                         ↓                                   │
│  Phase 4: CI/CD 실행                                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Playwright                                          │   │
│  │ → 지속적 회귀 테스트                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                         ↓                                   │
│  Phase 5: 자동 복구 (실패 시)                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Playwright Test Agents (Healer)                     │   │
│  │ → 실패 분석 → 셀렉터 수정 → 재실행                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 테스트 데이터 관리 전략

### 디렉토리 구조

```
e2e/
├── fixtures/           # 테스트 데이터
│   ├── common/
│   │   └── users.json        # 사용자 계정
│   ├── statistics/
│   │   ├── t-test.csv        # t검정용
│   │   ├── anova.csv         # ANOVA용
│   │   ├── correlation.csv   # 상관분석용
│   │   ├── regression.csv    # 회귀분석용
│   │   └── chi-square.csv    # 카이제곱용
│   └── env/
│       ├── dev.json
│       └── staging.json
├── specs/              # Planner 출력 (마크다운)
├── tests/              # Generator 출력 (spec.ts)
├── utils/
│   ├── data-loader.ts  # CSV/JSON 로더
│   └── factories/      # 데이터 팩토리
└── playwright.config.ts
```

### CSV 데이터 설계 원칙

```typescript
// utils/data-loader.ts
import { parse } from 'csv-parse/sync';
import { readFileSync } from 'fs';

export function loadTestData<T>(filename: string): T[] {
  const csvPath = `e2e/fixtures/statistics/${filename}`;
  const csvContent = readFileSync(csvPath, 'utf-8');
  return parse(csvContent, {
    columns: true,
    skip_empty_lines: true,
    cast: true  // 숫자 자동 변환
  });
}
```

### 메서드별 CSV 스키마

| 메서드 | 파일명 | 필수 컬럼 | 예시 |
|--------|--------|----------|------|
| t-test | `t-test.csv` | group, value | `A,10.5` |
| ANOVA | `anova.csv` | group, value | `Control,25.3` |
| Correlation | `correlation.csv` | var1, var2, ... | `1.2,3.4,5.6` |
| Regression | `regression.csv` | dependent, independent1, ... | `y,x1,x2` |
| Chi-square | `chi-square.csv` | category1, category2 | `Male,Yes` |
| Survival | `survival.csv` | time, event, covariate | `12,1,age` |

---

## 🎯 통계 플랫폼 적용 계획

### 우선순위 (48개 → 10개 먼저)

| 순위 | 메서드 | 이유 | 데이터 타입 |
|------|--------|------|------------|
| 🔴 1 | t-test | 가장 기본, 가장 많이 사용 | 수치형 2그룹 |
| 🔴 2 | anova | 그룹 비교 핵심 | 수치형 3+그룹 |
| 🔴 3 | correlation | 상관분석 핵심 | 수치형 2개+ |
| 🔴 4 | regression | 회귀분석 핵심 | 종속+독립 |
| 🔴 5 | chi-square | 범주형 핵심 | 범주형 2개 |
| 🟡 6 | mann-whitney | 비모수 대표 | 수치형 2그룹 |
| 🟡 7 | wilcoxon | 대응 비모수 | 수치형 쌍 |
| 🟡 8 | kruskal-wallis | 비모수 ANOVA | 수치형 3+그룹 |
| 🟡 9 | pca | 다변량 대표 | 수치형 다변량 |
| 🟡 10 | descriptive | 기초 통계 | 수치형 |

### 공통 테스트 플로우 템플릿

```typescript
// e2e/templates/statistics-flow.template.ts
export const statisticsTestFlow = {
  steps: [
    '1. 페이지 접속 (/statistics/{method})',
    '2. 데이터 업로드 (CSV 또는 샘플 데이터)',
    '3. 변수 선택 (종속/독립/그룹)',
    '4. 옵션 설정 (신뢰수준, 사후검정 등)',
    '5. 분석 실행 (버튼 클릭)',
    '6. 결과 대기 (로딩 완료)',
    '7. 결과 검증'
  ],

  assertions: {
    loading: 'Pyodide 로딩 완료',
    noError: '에러 메시지 없음',
    resultTable: '결과 테이블 표시',
    pValue: 'p-value 범위 (0 ≤ p ≤ 1)',
    chart: '차트 렌더링 (선택)',
    interpretation: 'AI 해석 표시 (선택)'
  }
};
```

---

## 🚀 단계별 실행 가이드

### Step 1: 환경 설정

```bash
# Playwright 설치
cd statistical-platform
pnpm add -D @playwright/test

# Test Agents 초기화 (Claude Code 사용)
npx playwright init-agents --loop=claude

# Agent Browser 전역 설치 (선택)
npm install -g agent-browser
agent-browser install
```

### Step 2: 테스트 데이터 준비

```bash
# fixtures 디렉토리 생성
mkdir -p e2e/fixtures/statistics

# 샘플 데이터 생성 또는 복사
# → 각 메서드별 CSV 파일 준비
```

### Step 3: Planner로 테스트 계획 생성

```bash
# Claude Code에서 Planner 실행
# → specs/*.md 자동 생성
```

### Step 4: Generator로 테스트 코드 생성

```bash
# Claude Code에서 Generator 실행
# → tests/*.spec.ts 자동 생성
```

### Step 5: CI/CD 통합

```yaml
# .github/workflows/e2e.yml
name: E2E Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm exec playwright install --with-deps
      - run: pnpm exec playwright test
```

---

## 📚 참조 문서

### 공식 문서
- [Playwright Test Agents](https://playwright.dev/docs/test-agents)
- [Playwright Best Practices](https://playwright.dev/docs/best-practices)
- [Playwright MCP Server - GitHub](https://github.com/microsoft/playwright-mcp)
- [Agent Browser GitHub](https://github.com/vercel-labs/agent-browser)

### Claude Code + Playwright
- [Write automated tests with Claude Code using Playwright Agents - Shipyard](https://shipyard.build/blog/playwright-agents-claude-code/)
- [Playwright Agents: Planner, Generator, and Healer in Action - DEV](https://dev.to/playwright/playwright-agents-planner-generator-and-healer-in-action-5ajh)

### 모범 사례
- [E2E Testing Guide - Microsoft](https://microsoft.github.io/code-with-engineering-playbook/automated-testing/e2e-testing/)
- [Test Data Management - Momentic](https://momentic.ai/resources/the-definitive-guide-to-playwright-test-data-management-strategies)
- [Data-Driven Testing with CSV](https://blog.nashtechglobal.com/data-driven-testing-with-playwright-using-json-and-csv/)

### 업계 동향 (2026)
- [E2E Testing Best Practices 2026 - BrowserStack](https://www.browserstack.com/guide/playwright-best-practices)
- [Playwright MCP AI Test Automation](https://www.testleaf.com/blog/playwright-mcp-ai-test-automation-2026/)
- [The Autonomous QA Revolution: Playwright Test Agents Explained](https://medium.com/@kapilkumar080/the-autonomous-qa-revolution-playwright-test-agents-explained-df49194f1603)

---

## ✅ 체크리스트

### 프로젝트 초기 설정
- [ ] Playwright 설치
- [ ] Test Agents 초기화 (`--loop` 옵션 지정)
- [ ] fixtures 디렉토리 구조 생성
- [ ] 메서드별 테스트 데이터 CSV 준비

### 테스트 작성
- [ ] Planner로 테스트 계획 생성
- [ ] 계획 검토 및 수정
- [ ] Generator로 테스트 코드 생성
- [ ] 로컬 실행 및 검증

### CI/CD 통합
- [ ] GitHub Actions 워크플로우 설정
- [ ] 환경별 데이터 파일 분리
- [ ] 실패 알림 설정
- [ ] Healer 자동 수정 워크플로우 (선택)

---

**Updated**: 2026-01-30
**Author**: Claude Code
**Version**: 1.1 (VS Code + Claude Code 확장 가이드 추가)